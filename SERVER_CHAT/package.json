{
  "name": "SERVER_CHAT",
  "version": "0.0.1",
  "private": "true",
  "dependencies": {
    "express": "*", 
    "socket.io": "*", // open port for transfer client and server
    // save client's file on server
    "socketio-file-upload": "^0.4.4"
  }
}
